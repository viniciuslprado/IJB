{
  "name": "ijb-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "speed-insights": "npm --prefix frontend run speed-insights",
    "install:frontend": "npm --prefix frontend install --include=dev",
    "start": "npm run dev"
  }
}
